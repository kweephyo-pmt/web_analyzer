interface:
  display_name: "TBS Web Analyzer"
  short_description: "Repo-specific patterns and workflows for TBS_web-analyzer"
  default_prompt: "Use the TBS_web-analyzer repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true